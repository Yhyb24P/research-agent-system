# ADR-002 — Protocols as Adapters

Status: Accepted

Internal domain objects and native services do not depend on external agent protocol types. Protocol lifecycle and object semantics are interoperability concerns, not internal workflow truth. Core tests must run with all adapter packages absent.
